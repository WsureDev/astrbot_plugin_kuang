{
  "box_count": {
    "description": "每张图输出的线框数量",
    "hint": "默认最多输出 5 个框。开启随机补框时，YOLO26n 识别不足会自动补齐；关闭后只保留识别出的框。",
    "type": "int",
    "default": 5
  },
  "confidence_threshold": {
    "description": "YOLO26n 识别置信度阈值",
    "hint": "默认 0.25。调高会减少误检，调低会增加候选框数量。",
    "type": "float",
    "default": 0.25
  },
  "detector_iou_threshold": {
    "description": "YOLO26n 去重 IoU 阈值",
    "hint": "默认 0.45。值越小越容易去掉重叠框。",
    "type": "float",
    "default": 0.45
  },
  "model_input_size": {
    "description": "YOLO26n 输入尺寸",
    "hint": "默认 640。通常保持默认即可。",
    "type": "int",
    "default": 640
  },
  "detector_backend": {
    "description": "识别器后端",
    "hint": "默认使用 yolo26n。高级用法下也支持 cascade_yolo26n_anime，用于开启二次元补强兜底。",
    "type": "string",
    "default": "yolo26n"
  },
  "model_path": {
    "description": "YOLO26n 模型文件路径",
    "hint": "留空时使用插件数据目录下的 models/yolo26n.onnx。若文件不存在且开启自动下载，将自动拉取官方模型。",
    "type": "string",
    "default": ""
  },
  "model_url": {
    "description": "YOLO26n 模型下载地址",
    "hint": "默认使用官方 Ultralytics 资源地址。仅在 auto_download_model 开启且本地缺少模型文件时使用。",
    "type": "string",
    "default": "https://github.com/ultralytics/assets/releases/download/v8.4.0/yolo26n.onnx"
  },
  "auto_download_model": {
    "description": "自动下载 YOLO26n 模型",
    "hint": "开启后，首次缺少模型文件时会自动下载到插件数据目录。",
    "type": "bool",
    "default": true
  },
  "enable_random_boxes": {
    "description": "启用随机补框",
    "hint": "默认开启。关闭后只保留 YOLO26n 识别到的框，不再补齐到固定数量。",
    "type": "bool",
    "default": true
  },
  "enable_anime_fallback": {
    "description": "启用二次元检测兜底",
    "hint": "默认关闭。开启后，当 yolo26n 的人形检测结果较少时，会尝试使用动漫部位 YOLO 模型做第二级补强。",
    "type": "bool",
    "default": false
  },
  "anime_model_path": {
    "description": "动漫 YOLO 模型文件路径",
    "hint": "留空时默认使用插件数据目录下的 models/anime_yolo.onnx。当前默认模型是二次元部位检测，类别为 Head / Torso / Legs。",
    "type": "string",
    "default": ""
  },
  "anime_model_url": {
    "description": "动漫 YOLO 模型下载地址",
    "hint": "仅在开启二次元检测兜底且 anime_auto_download_model 为 true 时使用。默认使用现成的 Hugging Face ONNX 地址。",
    "type": "string",
    "default": "https://huggingface.co/laowanglaowang/yolov11m_anime_Image_segmentation/resolve/main/best.onnx"
  },
  "anime_auto_download_model": {
    "description": "自动下载动漫 YOLO 模型",
    "hint": "开启后，若本地缺少动漫模型文件，会尝试从 anime_model_url 下载。",
    "type": "bool",
    "default": true
  },
  "anime_confidence_threshold": {
    "description": "动漫 YOLO 识别置信度阈值",
    "hint": "默认 0.25。用于 Head / Torso / Legs 二次元补强模型，建议按实际模型表现微调。",
    "type": "float",
    "default": 0.25
  },
  "anime_detector_iou_threshold": {
    "description": "动漫 YOLO 去重 IoU 阈值",
    "hint": "默认 0.45。仅影响动漫补强模型内部的去重。",
    "type": "float",
    "default": 0.45
  },
  "anime_model_input_size": {
    "description": "动漫 YOLO 输入尺寸",
    "hint": "默认 640。按你使用的动漫 ONNX 模型输入尺寸调整。",
    "type": "int",
    "default": 640
  },
  "anime_fallback_trigger_count": {
    "description": "触发动漫兜底的主检测数量阈值",
    "hint": "默认 2。主 yolo26n 的 person 检测数量少于这个值时，才会运行动漫补强模型。",
    "type": "int",
    "default": 2
  },
  "anime_merge_iou_threshold": {
    "description": "主检测与动漫补强合并 IoU 阈值",
    "hint": "默认 0.45。值越小越容易去掉主模型与动漫模型之间的重叠框。",
    "type": "float",
    "default": 0.45
  },
  "line_width": {
    "description": "白色线框基础线宽",
    "hint": "数值越大线条越粗。实际绘制时会结合图片尺寸做最小值修正。",
    "type": "int",
    "default": 3
  },
  "line_alpha": {
    "description": "白色线框透明度",
    "hint": "取值建议 0-255，默认 220。",
    "type": "int",
    "default": 220
  },
  "random_width_ratio_min": {
    "description": "随机框最小宽高比",
    "hint": "按 FPS 透视框风格使用偏细长矩形。这里是宽/高的最小值。",
    "type": "float",
    "default": 0.28
  },
  "random_width_ratio_max": {
    "description": "随机框最大宽高比",
    "hint": "按 FPS 透视框风格使用偏细长矩形。这里是宽/高的最大值。",
    "type": "float",
    "default": 0.52
  },
  "random_height_ratio_min": {
    "description": "随机框最小高度占图比例",
    "hint": "默认允许小框下探到大框约 1/6 的量级，用来模拟 FPS 视角里的远近差异。",
    "type": "float",
    "default": 0.09
  },
  "random_height_ratio_max": {
    "description": "随机框最大高度占图比例",
    "hint": "默认随机框高度不会过大，避免盖住整张图。",
    "type": "float",
    "default": 0.55
  },
  "avatar_url_template": {
    "description": "头像 URL 模板",
    "hint": "留空时，若发送者 ID 为纯数字，则默认使用 QQ 头像地址。可使用 {user_id}、{platform_name}、{platform_id} 变量。",
    "type": "string",
    "default": ""
  },
  "debug_mode": {
    "description": "是否打印 debug 级别日志",
    "hint": "开启后本插件的调试日志会以 INFO 形式输出（前缀 [DEBUG][astrbot_plugin_kuang]），不会修改 AstrBot 全局日志级别，也不影响其他插件。",
    "type": "bool",
    "default": false
  }
}
